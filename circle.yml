machine:
  node:
    version: 6.2.2

dependencies:
  override:
    - npm install

test:
  pre:
    - npm run relint

deployment:
  staging:
    branch: master
    heroku:
      appname: kclmodules
    commands:
      - npm run deploy
